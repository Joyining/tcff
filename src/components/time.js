import React, { Component } from 'react';

class Time extends Component {
  render() {
    return (
        <section className="container">
            場次表
        </section>
    );
  }
}

export default Time;
